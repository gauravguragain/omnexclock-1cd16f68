import { FormEvent, useEffect, useMemo, useState } from "react";
import MenuBookPicker from "./MenuBookPicker";
import { supabase } from "@/integrations/supabase/client"; import { useAuth } from "@/contexts/AuthContext";
import { Button } from "@/components/ui/button"; import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"; import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { Badge } from "@/components/ui/badge"; import { Checkbox } from "@/components/ui/checkbox";
import { ArrowRight, Bot, CalendarPlus, Check, Clock, Download, Mail, Phone, Plus, X } from "lucide-react"; import { toast } from "sonner"; import { format } from "date-fns";
import venueBanner from "@/assets/regal-venue-banner.jpg"; import OptionSelect from "./OptionSelect"; import DateField from "./DateField"; import { TimeDropdownPicker } from "@/components/TimeDropdownPicker";
const INTERACTION_TYPES=[{value:"phone_call",label:"Phone call"},{value:"email",label:"Email"},{value:"in_person",label:"In person"},{value:"message",label:"Message"}];
export const COURSE_CATEGORY:Record<string,string>={"Entrees (Veg)":"entrees_veg","Entrees (Non-veg)":"entrees_nonveg","Veg Mains":"veg_mains","Non-veg Mains":"nonveg_mains","Sides":"sides","Dessert":"dessert","Kids Menu":"kids_menu"};
export const DISH_COURSES=["Entrees (Veg)","Entrees (Non-veg)","Veg Mains","Non-veg Mains","Sides","Dessert","Kids Menu"];
import type { CrmInspection, CrmInteraction, CrmLead, CrmOption, CrmTask } from "./types"; import { CRM_LEAD_STATUSES, prettyCrmValue } from "./types"; import { buildBookingConfirmationPdf } from "@/lib/bookingConfirmationPdf"; import RunsheetTab from "./RunsheetTab"; import LeadStakeholdersTab from "./LeadStakeholdersTab";

export default function LeadDetailDialog({ lead, open, onOpenChange, initialTab, options, interactions, inspections, tasks, menuItems, booking, businessName, onSaved }:{ lead:CrmLead|null; open:boolean; onOpenChange:(v:boolean)=>void; initialTab?:string; options:CrmOption[]; interactions:CrmInteraction[]; inspections:CrmInspection[]; tasks:CrmTask[]; menuItems:any[]; booking:any; businessName:string; onSaved:()=>void }) {
  const { user } = useAuth(); const [busy,setBusy]=useState(false); const [ai,setAi]=useState<any>(null); const [selectedMenu,setSelectedMenu]=useState<string[]>([]); const [customItems,setCustomItems]=useState<{key:string;name:string;pricePerHead:number;flatPrice:number}[]>([]); const [guestOverride,setGuestOverride]=useState<number|null>(null);
  const [dishes,setDishes]=useState<{key:string;course:string;name:string;pkg?:string}[]>([]);
  const [kids,setKids]=useState({enabled:false,count:"",price:""}); const [kidDraft,setKidDraft]=useState("");
  const [manual,setManual]=useState<{key:string;name:string;pricePerHead:number;flatPrice:number}[]>([]); const [manualDraft,setManualDraft]=useState({name:"",pricePerHead:"",flatPrice:""});
  const [bookPackages,setBookPackages]=useState<any[]>([]);
  useEffect(()=>{if(!open||!lead)return;(async()=>{const bid=lead.business_id;const q=(t:string)=>(supabase.from(t as any) as any).select("*").eq("business_id",bid);const[p,b,c,ci,d]=await Promise.all([q("crm_packages").eq("active",true),q("crm_menu_books"),q("crm_package_courses").order("sort_order"),q("crm_package_course_items"),q("crm_dishes")]);setBookPackages((p.data||[]).map((x:any)=>({...x,book:(b.data||[]).find((y:any)=>y.id===x.book_id)?.name||"Menu",courses:(c.data||[]).filter((y:any)=>y.package_id===x.id).map((y:any)=>({...y,dishes:(ci.data||[]).filter((z:any)=>z.course_id===y.id&&z.dish_id).map((z:any)=>(d.data||[]).find((w:any)=>w.id===z.dish_id)).filter(Boolean)}))})));})();},[open,lead?.id]);
  const loadBookPackage=(p:any,picked:{course:string;dish:any}[],pricePerHead:number)=>{const pkg=`${Date.now()}`;setCustomItems(v=>[...v,{key:pkg,name:p.name,pricePerHead:Number(pricePerHead||0),flatPrice:0}]);setDishes(v=>[...v,...picked.map((x,i)=>({key:`${pkg}-${i}`,course:x.course.trim(),name:x.dish.name,pkg}))]);toast.success(`${p.name} loaded`);};
  const removePackage=(key:string)=>{setCustomItems(v=>v.filter(c=>c.key!==key));setDishes(v=>v.filter(d=>(d as any).pkg?((d as any).pkg!==key):!key.startsWith("l")));};
  const [corkage,setCorkage]=useState({enabled:false,perHead:"",flat:""}); const [extras,setExtras]=useState({dietary:"",allergies:"",beverage:""}); const [beverageChoice,setBeverageChoice]=useState("");
  const [liveStalls,setLiveStalls]=useState<{key:string;name:string;pricePerHead:number;flatPrice:number;startTime:string;endTime:string}[]>([]); const [stallDraft,setStallDraft]=useState({name:"",pricePerHead:"",flatPrice:"",startTime:"17:30",endTime:"18:30"}); const [stallsRequired,setStallsRequired]=useState(false);
  const [insDate,setInsDate]=useState(""); const [insStart,setInsStart]=useState("10:00"); const [insEnd,setInsEnd]=useState("11:00"); const [followDate,setFollowDate]=useState(""); const [followTime,setFollowTime]=useState("09:00"); const [bookStart,setBookStart]=useState(String(booking?.start_time||"18:00").slice(0,5)); const [bookEnd,setBookEnd]=useState("23:00"); const [durationHours,setDurationHours]=useState<number|string>(booking?.duration_minutes?Number(booking.duration_minutes)/60:5);
  const timeToMin=(t:string)=>{const[h,m]=t.split(":").map(Number);return h*60+m;};
  const minToTime=(m:number)=>{const hh=Math.floor(m/60)%24;const mm=m%60;return `${String(hh).padStart(2,"0")}:${String(mm).padStart(2,"0")}`;};
  const prettyTime=(value:string)=>{const[h,m]=value.split(":").map(Number);return `${h%12===0?12:h%12}:${String(m||0).padStart(2,"0")} ${h>=12?"PM":"AM"}`;};
  useEffect(()=>{if(booking?.start_time)setBookStart(String(booking.start_time).slice(0,5));},[booking?.start_time]);
  useEffect(()=>{if(booking?.duration_minutes)setDurationHours(Number(booking.duration_minutes)/60);},[booking?.duration_minutes]);
  useEffect(()=>{const endMin=timeToMin(bookStart)+Number(durationHours||0)*60;setBookEnd(minToTime(endMin));},[bookStart,durationHours]);
  const venueOptions=options.filter(o=>o.option_type==="venue_space"&&o.active); const beverageOptions=options.filter(o=>o.option_type==="beverage_package"&&o.active); const liveStallOptions=options.filter(o=>o.option_type==="live_stall"&&o.active); const leadInteractions=interactions.filter(i=>i.lead_id===lead?.id); const leadInspections=inspections.filter(i=>i.lead_id===lead?.id); const leadTasks=tasks.filter(i=>i.lead_id===lead?.id);
  const guests=guestOverride??Number(lead?.estimated_guest_count||1);
  const chosenItems=useMemo(()=>menuItems.filter(i=>selectedMenu.includes(i.id)),[menuItems,selectedMenu]);
  const lineTotal=(pph:number,flat:number)=>Number(flat||0)+Number(pph||0)*guests;
  const corkageTotal=corkage.enabled?Number(corkage.flat||0)+Number(corkage.perHead||0)*guests:0;
  const stallTotal=useMemo(()=>stallsRequired?liveStalls.reduce((sum,i)=>sum+lineTotal(i.pricePerHead,i.flatPrice),0):0,[liveStalls,stallsRequired,guests]);
  const packageTotal=useMemo(()=>chosenItems.reduce((sum,i)=>sum+lineTotal(Number(i.price_per_head||0),Number(i.flat_price||0)),0)+customItems.reduce((sum,i)=>sum+lineTotal(i.pricePerHead,i.flatPrice),0),[chosenItems,customItems,guests]);
  const kidsTotal=kids.enabled?Number(kids.price||0)*Number(kids.count||0):0;
  const manualTotal=manual.reduce((sum,i)=>sum+lineTotal(i.pricePerHead,i.flatPrice),0);
  const beverageOption=beverageOptions.find(o=>o.value===beverageChoice);
  const beverageTotal=beverageOption?lineTotal(Number(beverageOption.price_per_head||0),Number(beverageOption.flat_price||0)):0;
  const total=packageTotal+corkageTotal+stallTotal+kidsTotal+manualTotal+beverageTotal;
  const kidsCatalogue=Array.from(new Map(bookPackages.flatMap((p:any)=>(p.courses||[]).filter((c:any)=>/kid/i.test(c.name)).flatMap((c:any)=>c.dishes||[])).map((d:any)=>[d.name,d])).values());
  const dishCategories=new Set(Object.values(COURSE_CATEGORY));
  const groupedMenu=useMemo(()=>{const map=new Map<string,any[]>();menuItems.filter(i=>!dishCategories.has(i.category)).forEach(i=>{const key=i.category||"Other";map.set(key,[...(map.get(key)||[]),i]);});return Array.from(map.entries());},[menuItems]);
  useEffect(()=>{if(!lead?.id||!open)return;let cancelled=false;(async()=>{
    const{data:sel}=await supabase.from("crm_menu_selections").select("*").eq("lead_id",lead.id).maybeSingle();
    if(cancelled||!sel)return;const s:any=sel;
    if(s.guest_count)setGuestOverride(Number(s.guest_count));
    setCorkage({enabled:!!s.corkage_enabled,perHead:s.corkage_per_head!=null?String(s.corkage_per_head):"",flat:s.corkage_flat!=null?String(s.corkage_flat):""});
    setExtras({dietary:s.dietary_requirements||"",allergies:s.allergies||"",beverage:s.beverage_package||""});setBeverageChoice(s.beverage_package||"");
    const{data:items}=await supabase.from("crm_menu_selection_items").select("*").eq("selection_id",s.id);
    if(cancelled)return;const rows:any[]=items||[];
    setSelectedMenu(rows.filter(i=>i.menu_item_id).map(i=>i.menu_item_id));
    setCustomItems(rows.filter(i=>!i.menu_item_id&&i.course==="package").map((i,n)=>({key:`l${n}`,name:i.item_name,pricePerHead:Number(i.price_per_head||0),flatPrice:Number(i.flat_price||0)})));
    const stalls=rows.filter(i=>i.course==="live_stall").map((i,n)=>({key:`v${n}`,name:i.item_name,pricePerHead:Number(i.price_per_head||0),flatPrice:Number(i.flat_price||0),startTime:String(i.service_start_time||"17:30").slice(0,5),endTime:String(i.service_end_time||"18:30").slice(0,5)}));
    setLiveStalls(stalls); setStallsRequired(stalls.length>0);
    const k=rows.find(i=>i.course==="kids_package");setKids(k?{enabled:true,count:String(k.quantity||""),price:k.price_per_head!=null?String(k.price_per_head):""}:{enabled:false,count:"",price:""});
    setManual(rows.filter(i=>i.course==="manual").map((i,n)=>({key:`m${n}`,name:i.item_name,pricePerHead:Number(i.price_per_head||0),flatPrice:Number(i.flat_price||0)})));
    setDishes(rows.filter(i=>!i.menu_item_id&&i.course&&!["package","live_stall","kids_package","manual"].includes(i.course)).map((i,n)=>({key:`d${n}`,course:i.course,name:i.item_name})));
  })();return()=>{cancelled=true;};},[lead?.id,open]);
  if(!lead)return null;
  const addInteraction=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const notes=String(f.get("notes"));const {error}=await supabase.from("crm_interactions").insert({business_id:lead.business_id,lead_id:lead.id,interaction_type:String(f.get("type")),notes,duration_minutes:f.get("duration")?Number(f.get("duration")):null,follow_up_required:f.get("follow_up")==="on",follow_up_at:followDate?`${followDate}T${followTime}`:null,shareable_feedback:f.get("shareable")==="on",logged_by:user?.id} as any);setBusy(false);if(error)toast.error(error.message);else{toast.success("Interaction logged");(e.target as HTMLFormElement).reset();setFollowDate("");onSaved();}};
  const analyse=async(notes:string)=>{if(notes.trim().length<10){toast.error("Add more notes first");return;}setBusy(true);const{data,error}=await supabase.functions.invoke("crm-ai-notes",{body:{businessId:lead.business_id,notes}});setBusy(false);if(error)toast.error("Could not analyse notes");else setAi(data);};
  const addInspection=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);if(!insDate){toast.error("Pick an inspection date");return;}if(insEnd<=insStart){toast.error("End time must be after the start time");return;}const start=`${insDate}T${insStart}`;const end=`${insDate}T${insEnd}`;const venue=String(f.get("venue_space"));const conflict=inspections.find(i=>i.venue_space===venue&&i.status!=="cancelled"&&i.starts_at&&i.ends_at&&start<i.ends_at&&end>i.starts_at);if(conflict&&!confirm("This overlaps another inspection in the same venue. Save anyway?"))return;const{error}=await supabase.from("crm_inspections").insert({business_id:lead.business_id,lead_id:lead.id,starts_at:start,ends_at:end,status:"confirmed",venue_space:venue,pre_notes:f.get("notes")?String(f.get("notes")):null,assigned_to:user?.id,created_by:user?.id} as any);if(error)toast.error(error.message);else{await supabase.from("crm_leads").update({status:"inspection_booked"}).eq("id",lead.id);toast.success("Inspection booked");onSaved();}};
  const saveMenu=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);
    const pendingStallName=String((document.querySelector('input[name="live_stall_draft"]') as HTMLInputElement|null)?.value||"").trim();
    const pendingStall=stallsRequired&&pendingStallName&&!liveStalls.some(v=>v.name===pendingStallName)?[{key:`s${Date.now()}`,name:pendingStallName,pricePerHead:Number(stallDraft.pricePerHead||0),flatPrice:Number(stallDraft.flatPrice||0),startTime:stallDraft.startTime,endTime:stallDraft.endTime}]:[];
    const allCustom=customItems; const allStalls=stallsRequired?[...liveStalls,...pendingStall]:[];
    if(pendingStall.length){setLiveStalls(allStalls);setStallDraft({name:"",pricePerHead:"",flatPrice:"",startTime:"17:30",endTime:"18:30"});}
    const stallsSum=allStalls.reduce((sum,i)=>sum+lineTotal(i.pricePerHead,i.flatPrice),0);
    const grandTotal=chosenItems.reduce((sum,i)=>sum+lineTotal(Number(i.price_per_head||0),Number(i.flat_price||0)),0)+allCustom.reduce((sum,i)=>sum+lineTotal(i.pricePerHead,i.flatPrice),0)+corkageTotal+stallsSum+kidsTotal+manualTotal+beverageTotal;
    const{data:selection,error}=await supabase.from("crm_menu_selections").upsert({business_id:lead.business_id,lead_id:lead.id,guest_count:guests,dietary_requirements:extras.dietary||null,allergies:extras.allergies||null,beverage_package:f.get("beverage")?String(f.get("beverage")):null,package_name:allCustom[0]?.name||chosenItems[0]?.name||null,package_price_per_head:allCustom[0]?.pricePerHead||chosenItems[0]?.price_per_head||null,corkage_enabled:corkage.enabled,corkage_per_head:corkage.enabled&&corkage.perHead?Number(corkage.perHead):null,corkage_flat:corkage.enabled&&corkage.flat?Number(corkage.flat):null,total_estimate:grandTotal,updated_by:user?.id} as any,{onConflict:"lead_id"}).select("id").single();if(error||!selection){toast.error(error?.message||"Could not save menu");return;}await supabase.from("crm_menu_selection_items").delete().eq("selection_id",selection.id);const rows=[...chosenItems.map(i=>({business_id:lead.business_id,selection_id:selection.id,menu_item_id:i.id,item_name:i.name,price_per_head:i.price_per_head,flat_price:i.flat_price,course:"package"})),...allCustom.map(i=>({business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:i.name,price_per_head:i.pricePerHead||null,flat_price:i.flatPrice||null,course:"package"})),...(kids.enabled?[{business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:"Kids menu",price_per_head:Number(kids.price||0)||null,quantity:Math.max(1,Number(kids.count||0)),course:"kids_package"}]:[]),...(beverageOption&&beverageTotal>0?[{business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:`Beverage package: ${beverageOption.label}`,price_per_head:beverageOption.price_per_head||null,flat_price:beverageOption.flat_price||null,course:"beverage"}]:[]),...manual.map(i=>({business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:i.name,price_per_head:i.pricePerHead||null,flat_price:i.flatPrice||null,course:"manual"})),...dishes.filter(d=>kids.enabled||d.course!=="Kids Menu").map(d=>({business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:d.name,course:d.course})),...(allStalls.length?allStalls.map(i=>({business_id:lead.business_id,selection_id:selection.id,menu_item_id:null,item_name:i.name,price_per_head:i.pricePerHead||null,flat_price:i.flatPrice||null,course:"live_stall",service_start_time:i.startTime||null,service_end_time:i.endTime||null})):[])];if(rows.length)await supabase.from("crm_menu_selection_items").insert(rows as any);await supabase.from("crm_leads").update({status:"menu_selected",estimated_value:grandTotal}).eq("id",lead.id);toast.success("Menu selection saved");onSaved();};
  const createBooking=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const hours=Number(f.get("duration"));const durationMinutes=Math.round(hours*60);const{error}=await supabase.from("crm_bookings").upsert({business_id:lead.business_id,lead_id:lead.id,event_date:String(f.get("event_date")),start_time:String(f.get("start_time")),duration_minutes:durationMinutes,guest_count:Number(f.get("guests")),venue_space:String(f.get("venue")),total_amount:Number(f.get("total")),deposit_amount:Number(f.get("deposit")),deposit_due_date:f.get("deposit_due")?String(f.get("deposit_due")):null,balance_due_date:f.get("balance_due")?String(f.get("balance_due")):null,created_by:user?.id,updated_by:user?.id} as any,{onConflict:"lead_id"});if(error)toast.error(error.message);else{toast.success("Booking prepared");onSaved();}};
  const downloadPdf=()=>{if(!booking)return;buildBookingConfirmationPdf({businessName,clientName:lead.full_name,eventType:prettyCrmValue(lead.event_type),eventDate:booking.event_date,eventTime:booking.start_time,durationMinutes:booking.duration_minutes,venueSpace:booking.venue_space,guestCount:booking.guest_count,menuSummary:[...chosenItems.map(i=>i.name),...customItems.map(i=>i.name)],totalAmount:Number(booking.total_amount),depositAmount:Number(booking.deposit_amount),depositDueDate:booking.deposit_due_date,balanceDueDate:booking.balance_due_date}).save(`${lead.full_name.replace(/\s+/g,"-")}-booking.pdf`);};
  const setStage=async(status:string)=>{const{error}=await supabase.from("crm_leads").update({status,last_contact_at:new Date().toISOString()}).eq("id",lead.id);if(error)toast.error(error.message);else{toast.success(`Moved to ${prettyCrmValue(status)}`);onSaved();}};
  const quickLog=async(type:string)=>{setBusy(true);const{error}=await supabase.from("crm_interactions").insert({business_id:lead.business_id,lead_id:lead.id,interaction_type:type,notes:`${prettyCrmValue(type)} logged from quick actions.`,follow_up_required:false,shareable_feedback:false,logged_by:user?.id} as any);await supabase.from("crm_leads").update({last_contact_at:new Date().toISOString(),status:lead.status==="new"?"contacted":lead.status}).eq("id",lead.id);setBusy(false);if(error)toast.error(error.message);else{toast.success("Contact logged");onSaved();}};
  const quickTask=async(title:string,days:number)=>{const due=new Date(Date.now()+days*86400000).toISOString();const{error}=await supabase.from("crm_tasks").insert({business_id:lead.business_id,lead_id:lead.id,title,task_type:"follow_up",due_at:due,priority:"medium",status:"open",automated:false,created_by:user?.id} as any);if(error)toast.error(error.message);else{toast.success("Reminder added");onSaved();}};
  const nextStep=lead.status==="new"?{label:"Call the client and log the conversation",action:()=>quickLog("phone_call")}:lead.status==="contacted"?{label:"Book a venue inspection",action:null}:lead.status==="inspection_booked"?{label:"Mark the inspection as completed",action:()=>setStage("inspected")}:lead.status==="inspected"?{label:"Build the menu and pricing",action:null}:lead.status==="menu_selected"?{label:"Send the invoice and mark as invoice sent",action:()=>setStage("invoice_sent")}:lead.status==="invoice_sent"?{label:"Record deposit received",action:()=>setStage("deposit_received")}:lead.status==="deposit_received"?{label:"Send the runsheet and mark as sent",action:()=>setStage("runsheet_sent")}:lead.status==="runsheet_sent"?{label:"Record full payment received",action:()=>setStage("full_payment_received")}:null;
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[94dvh] overflow-y-auto p-0 sm:max-w-5xl"><div className="relative h-44 overflow-hidden"><img src={venueBanner} alt="Warm venue interior" className="h-full w-full object-cover"/><div className="absolute inset-0 bg-background/65"/><DialogHeader className="absolute bottom-0 left-0 p-6"><div className="flex gap-2"><Badge>{prettyCrmValue(lead.status)}</Badge>{lead.calendly_source&&<Badge variant="outline">Calendly</Badge>}</div><DialogTitle className="mt-2 font-serif text-3xl text-foreground">{lead.full_name}</DialogTitle><p className="text-sm text-foreground/80">{lead.event_type&&prettyCrmValue(lead.event_type)} · {lead.estimated_guest_count||"—"} guests</p></DialogHeader></div>
  <div className="border-b border-border bg-muted/30 px-5 py-4">
    <div className="flex flex-wrap items-center gap-2">
      <Button size="sm" variant="outline" disabled={!lead.phone} asChild={!!lead.phone}>{lead.phone?<a href={`tel:${lead.phone}`}><Phone className="mr-2 h-4 w-4"/>Call</a>:<span><Phone className="mr-2 h-4 w-4"/>Call</span>}</Button>
      <Button size="sm" variant="outline" disabled={!lead.email} asChild={!!lead.email}>{lead.email?<a href={`mailto:${lead.email}`}><Mail className="mr-2 h-4 w-4"/>Email</a>:<span><Mail className="mr-2 h-4 w-4"/>Email</span>}</Button>
      <Button size="sm" variant="outline" disabled={busy} onClick={()=>quickLog("phone_call")}><Phone className="mr-2 h-4 w-4"/>Log call</Button>
      <Button size="sm" variant="outline" onClick={()=>quickTask(`Follow up with ${lead.full_name}`,2)}><Clock className="mr-2 h-4 w-4"/>Remind me in 2 days</Button>
      <select value={lead.status} onChange={e=>setStage(e.target.value)} className="h-9 rounded-md border border-input bg-background px-3 text-sm">{CRM_LEAD_STATUSES.map(s=><option key={s} value={s}>{prettyCrmValue(s)}</option>)}</select>
    </div>
    {nextStep&&<div className="mt-3 flex flex-wrap items-center gap-3 rounded-md border border-primary/30 bg-primary/5 px-3 py-2 text-sm"><ArrowRight className="h-4 w-4 text-primary"/><span>Next step: {nextStep.label}</span>{nextStep.action&&<Button size="sm" variant="secondary" onClick={nextStep.action}>Done</Button>}</div>}
  </div>
  <div className="p-5"><Tabs key={initialTab||"timeline"} defaultValue={initialTab||"timeline"}><TabsList className="h-auto w-full justify-start overflow-x-auto"><TabsTrigger value="timeline">Timeline</TabsTrigger><TabsTrigger value="inspection">Inspection</TabsTrigger><TabsTrigger value="menu">Menu</TabsTrigger><TabsTrigger value="booking">Confirmation</TabsTrigger><TabsTrigger value="stakeholders">Stakeholders</TabsTrigger><TabsTrigger value="runsheet">Runsheet</TabsTrigger></TabsList>
  <TabsContent value="stakeholders"><LeadStakeholdersTab leadId={lead.id} businessId={lead.business_id} /></TabsContent>
  <TabsContent value="timeline" className="space-y-5"><div className="flex flex-wrap gap-3 text-sm"><span className="flex items-center gap-1"><Phone className="h-4 w-4"/>{lead.phone||"No phone"}</span><span className="flex items-center gap-1"><Mail className="h-4 w-4"/>{lead.email||"No email"}</span></div>
    <form onSubmit={addInteraction} className="grid gap-3 rounded-md border border-border p-4"><div className="grid gap-3 sm:grid-cols-3"><OptionSelect name="type" options={INTERACTION_TYPES} defaultValue="phone_call" required/><Input name="duration" type="number" min="1" placeholder="Duration (minutes)"/><div className="flex gap-2"><DateField value={followDate} onChange={setFollowDate} placeholder="Follow-up date"/>{followDate&&<TimeDropdownPicker value={followTime} onChange={setFollowTime}/>}</div></div><Textarea id="crm-note" name="notes" required placeholder="Questions, decisions and next steps"/><div className="flex flex-wrap items-center gap-4"><label className="flex items-center gap-2 text-sm"><Checkbox name="follow_up"/> Follow-up required</label><label className="flex items-center gap-2 text-sm"><Checkbox name="shareable"/> Shareable feedback</label><Button type="button" variant="outline" size="sm" onClick={()=>analyse((document.getElementById("crm-note") as HTMLTextAreaElement)?.value||"")}><Bot className="mr-2 h-4 w-4"/>Suggest actions</Button><Button size="sm" disabled={busy}><Plus className="mr-2 h-4 w-4"/>Log interaction</Button></div></form>
    {ai&&<div className="rounded-md border border-primary/30 bg-primary/5 p-4 text-sm"><p className="font-semibold">AI suggestion</p><p className="mt-1">{ai.summary}</p>{ai.tasks?.map((task:any,index:number)=><p key={index} className="mt-1 text-muted-foreground">• {task.title}{task.suggested_date?` — ${task.suggested_date}`:""}</p>)}</div>}
    <div className="space-y-3">{leadInteractions.map(i=><div key={i.id} className="border-l-2 border-primary/40 pl-4"><div className="flex justify-between gap-3"><p className="font-medium">{prettyCrmValue(i.interaction_type)}</p><time className="text-xs text-muted-foreground">{format(new Date(i.occurred_at),"dd/MM/yyyy h:mm a")}</time></div><p className="mt-1 text-sm text-muted-foreground">{i.notes}</p></div>)}</div>
  </TabsContent>
  <TabsContent value="inspection" className="space-y-4"><form onSubmit={addInspection} className="grid gap-4 rounded-lg border border-border p-4 sm:grid-cols-2">
    <div className="space-y-1.5 sm:col-span-2"><Label>Inspection date</Label><DateField value={insDate} onChange={setInsDate} placeholder="Choose a date"/></div>
    <div className="space-y-1.5"><Label>Start time</Label><TimeDropdownPicker value={insStart} onChange={setInsStart}/></div>
    <div className="space-y-1.5"><Label>End time</Label><TimeDropdownPicker value={insEnd} onChange={setInsEnd}/></div>
    <div className="space-y-1.5"><Label>Venue space</Label><OptionSelect name="venue_space" options={venueOptions} required/></div>
    <div className="space-y-1.5"><Label>Pre-inspection notes</Label><Input name="notes"/></div>
    <Button className="sm:col-span-2"><CalendarPlus className="mr-2 h-4 w-4"/>Book inspection</Button>
  </form>
  {leadInspections.map(i=><div key={i.id} className="rounded-md border p-4"><div className="flex justify-between"><p className="font-medium">{i.venue_space}</p><Badge variant="outline">{prettyCrmValue(i.status)}</Badge></div><p className="text-sm text-muted-foreground">{i.starts_at?format(new Date(i.starts_at),"dd/MM/yyyy h:mm a"):"Proposed"}{i.ends_at?` – ${format(new Date(i.ends_at),"h:mm a")}`:""}</p></div>)}</TabsContent>
  <TabsContent value="menu"><form onSubmit={saveMenu} className="grid gap-5 lg:grid-cols-[1.4fr_1fr]">
    <div className="space-y-5">
      {groupedMenu.length?groupedMenu.map(([category,items])=><section key={category} className="space-y-3">
        <div className="flex items-center gap-3"><h3 className="font-serif text-lg">{category}</h3><span className="h-px flex-1 bg-border"/><span className="text-xs text-muted-foreground">{items.length} options</span></div>
        <div className="grid gap-3 sm:grid-cols-2">{items.map((item:any)=>{const on=selectedMenu.includes(item.id);return <button type="button" key={item.id} onClick={()=>setSelectedMenu(v=>on?v.filter(id=>id!==item.id):[...v,item.id])} className={`group rounded-lg border p-4 text-left transition-all ${on?"border-primary bg-primary/5 shadow-sm ring-1 ring-primary/40":"border-border hover:border-primary/40 hover:bg-muted/40"}`}>
          <div className="flex items-start justify-between gap-3"><span className="font-medium">{item.name}</span><span className={`flex h-5 w-5 shrink-0 items-center justify-center rounded-full border ${on?"border-primary bg-primary text-primary-foreground":"border-border"}`}>{on&&<Check className="h-3 w-3"/>}</span></div>
          {item.description&&<p className="mt-1 text-xs text-muted-foreground">{item.description}</p>}
          <p className="mt-3 text-sm text-primary">{item.price_per_head?`$${Number(item.price_per_head).toFixed(2)} per guest`:`$${Number(item.flat_price||0).toFixed(2)} flat`}</p>
          <p className="text-xs text-muted-foreground">Line total ${lineTotal(Number(item.price_per_head||0),Number(item.flat_price||0)).toFixed(2)}</p>
        </button>})}</div>
      </section>):<p className="text-sm text-muted-foreground">No menu packages in the catalogue yet. Add a package selection below.</p>}

      <section className="rounded-lg border border-dashed border-border p-4">
        <h3 className="font-serif text-lg">Package selection</h3>
        {bookPackages.length>0&&<MenuBookPicker packages={bookPackages} onAdd={loadBookPackage}/>}
        <p className="mt-1 text-xs text-muted-foreground">Pick a menu book and package above — dishes are chosen from each course's dropdown.</p>
      </section>


      <section className="space-y-3 rounded-lg border border-border p-4">
        <label className="flex items-center gap-2 text-sm font-medium"><Checkbox checked={kids.enabled} onCheckedChange={c=>setKids(v=>({...v,enabled:!!c}))}/> Kids menu required</label>
        {kids.enabled&&<>
          <div className="grid gap-2 sm:grid-cols-2">
            <div><Label className="text-xs">Number of kids</Label><Input type="number" min="0" value={kids.count} onChange={e=>setKids(v=>({...v,count:e.target.value}))}/></div>
            <div><Label className="text-xs">Price per kid ($)</Label><Input type="number" min="0" step="0.01" value={kids.price} onChange={e=>setKids(v=>({...v,price:e.target.value}))}/></div>
          </div>
          <div className="flex flex-wrap gap-1.5">{dishes.filter(d=>d.course==="Kids Menu").map(d=><Badge key={d.key} variant="outline" className="gap-1">{d.name}<button type="button" onClick={()=>setDishes(v=>v.filter(x=>x.key!==d.key))}><X className="h-3 w-3"/></button></Badge>)}{!dishes.some(d=>d.course==="Kids Menu")&&<p className="text-xs text-muted-foreground">No kids dishes yet.</p>}</div>
          <div className="grid gap-2">
            <select value="" className="h-10 rounded-md border border-input bg-background px-3 text-sm" onChange={e=>{const name=e.target.value;if(name&&!dishes.some(d=>d.course==="Kids Menu"&&d.name===name))setDishes(v=>[...v,{key:`k${Date.now()}`,course:"Kids Menu",name}]);}}><option value="">{kidsCatalogue.length?"Choose a kids dish":"No kids dishes in menu books"}</option>{kidsCatalogue.map((i:any)=><option key={i.id} value={i.name}>{i.name}</option>)}</select>
          </div>
          {kidsTotal>0&&<p className="text-xs text-muted-foreground">Kids menu adds ${kidsTotal.toFixed(2)} to the total.</p>}
        </>}
      </section>

      <section className="space-y-3 rounded-lg border border-border p-4">
        <p className="text-sm font-medium">Manual pricing</p>
        <p className="text-xs text-muted-foreground">Add any extra charge or discount (use a negative amount) — per guest and/or flat.</p>
        {manual.map(i=><div key={i.key} className="flex items-center justify-between gap-2 rounded-md bg-muted/40 px-2 py-1 text-sm"><span>{i.name}</span><span className="flex items-center gap-2"><span>${lineTotal(i.pricePerHead,i.flatPrice).toFixed(2)}</span><button type="button" onClick={()=>setManual(v=>v.filter(x=>x.key!==i.key))}><X className="h-3.5 w-3.5 text-muted-foreground hover:text-destructive"/></button></span></div>)}
        <div className="grid gap-2 sm:grid-cols-[2fr_1fr_1fr_auto]">
          <Input value={manualDraft.name} placeholder="Description" onChange={e=>setManualDraft({...manualDraft,name:e.target.value})}/>
          <Input value={manualDraft.pricePerHead} type="number" step="0.01" placeholder="$ per guest" onChange={e=>setManualDraft({...manualDraft,pricePerHead:e.target.value})}/>
          <Input value={manualDraft.flatPrice} type="number" step="0.01" placeholder="$ flat" onChange={e=>setManualDraft({...manualDraft,flatPrice:e.target.value})}/>
          <Button type="button" variant="secondary" size="icon" onClick={()=>{const name=manualDraft.name.trim();if(!name){toast.error("Add a description");return;}setManual(v=>[...v,{key:`m${Date.now()}`,name,pricePerHead:Number(manualDraft.pricePerHead||0),flatPrice:Number(manualDraft.flatPrice||0)}]);setManualDraft({name:"",pricePerHead:"",flatPrice:""});}}><Plus className="h-4 w-4"/></Button>
        </div>
      </section>

      <section className="space-y-3 rounded-lg border border-border p-4">
        <label className="flex items-center gap-2 text-sm font-medium"><Checkbox checked={stallsRequired} onCheckedChange={c=>setStallsRequired(!!c)}/> Live stalls required</label>
        {stallsRequired&&<>
          <p className="text-xs text-muted-foreground">Choose the stall and how it is charged — per person or a flat rate.</p>
          <div className="space-y-1">
            {liveStalls.map(stall=><div key={stall.key} className="flex items-center justify-between gap-2 rounded-md bg-muted/40 px-2 py-1 text-sm">
              <span>{stall.name}<span className="ml-2 text-xs text-muted-foreground">{prettyTime(stall.startTime)} – {prettyTime(stall.endTime)} · {stall.pricePerHead?`$${stall.pricePerHead.toFixed(2)} per guest`:""}{stall.pricePerHead&&stall.flatPrice?" + ":""}{stall.flatPrice?`$${stall.flatPrice.toFixed(2)} flat`:""}{!stall.pricePerHead&&!stall.flatPrice?"No extra charge":""}</span></span>
              <span className="flex items-center gap-2 whitespace-nowrap"><span>${lineTotal(stall.pricePerHead,stall.flatPrice).toFixed(2)}</span><button type="button" title="Remove stall" onClick={()=>setLiveStalls(v=>v.filter(x=>x.key!==stall.key))}><X className="h-3.5 w-3.5 text-muted-foreground hover:text-destructive"/></button></span>
            </div>)}
            {!liveStalls.length&&<p className="text-xs text-muted-foreground">No stalls added yet.</p>}
          </div>
          <div className="grid gap-2 sm:grid-cols-[2fr_1fr_1fr_auto]">
            <OptionSelect key={`stall-${liveStalls.length}`} name="live_stall_draft" options={liveStallOptions} defaultValue={stallDraft.name} emptyLabel="Choose a live stall" otherLabel="Other (type your own)" otherPlaceholder="Stall name"/>
            <Input value={stallDraft.pricePerHead} onChange={e=>setStallDraft({...stallDraft,pricePerHead:e.target.value})} type="number" min="0" step="0.01" placeholder="$ per person"/>
            <Input value={stallDraft.flatPrice} onChange={e=>setStallDraft({...stallDraft,flatPrice:e.target.value})} type="number" min="0" step="0.01" placeholder="$ flat rate"/>
            <Button type="button" variant="secondary" onClick={()=>{const field=document.querySelector('input[name="live_stall_draft"]') as HTMLInputElement|null;const name=(field?.value||"").trim();if(!name){toast.error("Choose or type a live stall");return;}if(stallDraft.endTime<=stallDraft.startTime){toast.error("Live stall end time must be after its start time");return;}setLiveStalls(v=>[...v,{key:`stall-${Date.now()}`,name,pricePerHead:Number(stallDraft.pricePerHead||0),flatPrice:Number(stallDraft.flatPrice||0),startTime:stallDraft.startTime,endTime:stallDraft.endTime}]);setStallDraft({name:"",pricePerHead:"",flatPrice:"",startTime:"17:30",endTime:"18:30"});}}><Plus className="h-4 w-4"/></Button>
          </div>
          <div className="grid gap-2 sm:grid-cols-2">
            <div className="space-y-1"><Label className="text-xs">Stall start time</Label><TimeDropdownPicker value={stallDraft.startTime} onChange={startTime=>setStallDraft({...stallDraft,startTime})}/></div>
            <div className="space-y-1"><Label className="text-xs">Stall end time</Label><TimeDropdownPicker value={stallDraft.endTime} onChange={endTime=>setStallDraft({...stallDraft,endTime})}/></div>
          </div>
          {stallTotal>0&&<p className="text-xs text-muted-foreground">Live stalls add ${stallTotal.toFixed(2)} to the total.</p>}
        </>}
      </section>
    </div>

    <aside className="space-y-4 self-start rounded-lg border border-border bg-muted/30 p-4 lg:sticky lg:top-4">
      <div><Label>Adult guests for pricing</Label><Input type="number" min="1" value={guests} onChange={e=>setGuestOverride(Number(e.target.value)||1)}/></div>
      <div className="space-y-2">
        <p className="text-sm font-semibold">Package selections</p>
        {!chosenItems.length&&!customItems.length&&<p className="text-sm text-muted-foreground">Nothing selected yet.</p>}
        {chosenItems.map(i=><div key={i.id} className="flex items-start justify-between gap-2 border-b border-border pb-2 text-sm"><span>{i.name}</span><span className="flex items-center gap-2 whitespace-nowrap"><span>${lineTotal(Number(i.price_per_head||0),Number(i.flat_price||0)).toFixed(2)}</span><button type="button" title="Remove item" onClick={()=>setSelectedMenu(v=>v.filter(id=>id!==i.id))}><X className="h-3.5 w-3.5 text-muted-foreground hover:text-destructive"/></button></span></div>)}
        {customItems.map(i=><div key={i.key} className="flex items-start justify-between gap-2 border-b border-border pb-2 text-sm"><span>{i.name}<span className="mt-1 flex items-center gap-1 text-xs text-muted-foreground">$<input type="number" min="0" step="0.01" value={i.pricePerHead||""} placeholder="0.00" onChange={e=>setCustomItems(v=>v.map(c=>c.key===i.key?{...c,pricePerHead:Number(e.target.value)||0}:c))} className="h-7 w-20 rounded border border-input bg-background px-2"/> per guest × {guests}</span></span><span className="flex items-center gap-2 whitespace-nowrap"><span>${lineTotal(i.pricePerHead,i.flatPrice).toFixed(2)}</span><button type="button" title="Remove item" onClick={()=>removePackage(i.key)}><X className="h-3.5 w-3.5 text-muted-foreground hover:text-destructive"/></button></span></div>)}
      </div>
      {dishes.filter(d=>d.course!=="Kids Menu").length>0&&<div className="space-y-1"><p className="text-sm font-semibold">Dishes</p>{Array.from(new Set(dishes.map(d=>d.course))).filter(c=>c!=="Kids Menu").map(c=>{const l=dishes.filter(d=>d.course===c);return l.length?<p key={c} className="text-xs"><span className="text-muted-foreground">{c}: </span>{l.map(d=><span key={d.key} className="mr-1 inline-flex items-center gap-0.5">{d.name}<button type="button" onClick={()=>setDishes(v=>v.filter(x=>x.key!==d.key))}><X className="h-3 w-3 text-muted-foreground"/></button></span>)}</p>:null;})}</div>}
      {stallsRequired&&!!liveStalls.length&&<div className="space-y-1"><p className="text-sm font-semibold">Live stalls</p>{liveStalls.map(i=><div key={i.key} className="flex justify-between gap-2 text-sm"><span>{i.name}</span><span>${lineTotal(i.pricePerHead,i.flatPrice).toFixed(2)}</span></div>)}</div>}
      <div><Label>Beverage package</Label><OptionSelect name="beverage" options={beverageOptions} defaultValue={extras.beverage} emptyLabel="Not selected" onChange={setBeverageChoice}/>{beverageOption&&beverageTotal>0&&<p className="mt-1 text-xs text-muted-foreground">{beverageOption.price_per_head?`$${Number(beverageOption.price_per_head).toFixed(2)} per guest × ${guests}`:""}{beverageOption.price_per_head&&beverageOption.flat_price?" + ":""}{beverageOption.flat_price?`$${Number(beverageOption.flat_price).toFixed(2)} flat`:""} — adds ${beverageTotal.toFixed(2)} to the total.</p>}</div>
      <div className="space-y-2 rounded-md border border-border bg-background/60 p-3">
        <label className="flex items-center gap-2 text-sm font-medium"><Checkbox checked={corkage.enabled} onCheckedChange={c=>setCorkage(v=>({...v,enabled:!!c}))}/> Host is bringing their own drinks (corkage)</label>
        {corkage.enabled&&<div className="grid gap-2 sm:grid-cols-2">
          <div><Label className="text-xs">Corkage per guest</Label><Input type="number" min="0" step="0.01" value={corkage.perHead} onChange={e=>setCorkage(v=>({...v,perHead:e.target.value}))}/></div>
          <div><Label className="text-xs">Flat corkage fee</Label><Input type="number" min="0" step="0.01" value={corkage.flat} onChange={e=>setCorkage(v=>({...v,flat:e.target.value}))}/></div>
        </div>}
        {corkage.enabled&&<p className="text-xs text-muted-foreground">Corkage adds ${corkageTotal.toFixed(2)} to the total.</p>}
      </div>
      <div><Label>Dietary requirements</Label><Textarea rows={2} value={extras.dietary} onChange={e=>setExtras(v=>({...v,dietary:e.target.value}))}/></div>
      <div><Label>Allergies</Label><Textarea rows={2} className="border-destructive/40" value={extras.allergies} onChange={e=>setExtras(v=>({...v,allergies:e.target.value}))}/></div>
      <div className="space-y-1 border-t border-border pt-3 text-sm">
        <div className="flex justify-between"><span className="text-muted-foreground">Menu packages</span><span>${packageTotal.toFixed(2)}</span></div>
        {stallsRequired&&<div className="flex justify-between"><span className="text-muted-foreground">Live stalls</span><span>${stallTotal.toFixed(2)}</span></div>}
        {kids.enabled&&<div className="flex justify-between"><span className="text-muted-foreground">Kids menu ({kids.count||0})</span><span>${kidsTotal.toFixed(2)}</span></div>}
        {manual.length>0&&<div className="flex justify-between"><span className="text-muted-foreground">Manual pricing</span><span>${manualTotal.toFixed(2)}</span></div>}
        {corkage.enabled&&<div className="flex justify-between"><span className="text-muted-foreground">Corkage</span><span>${corkageTotal.toFixed(2)}</span></div>}
        {beverageOption&&beverageTotal>0&&<div className="flex justify-between"><span className="text-muted-foreground">Beverage package ({beverageOption.label})</span><span>${beverageTotal.toFixed(2)}</span></div>}
      </div>
      <div className="border-t border-border pt-3"><p className="text-xs uppercase text-muted-foreground">Estimated total</p><p className="font-serif text-3xl">${total.toFixed(2)}</p><p className="text-xs text-muted-foreground">${(total/Math.max(guests,1)).toFixed(2)} per guest · {guests} guests</p></div>
      <Button className="w-full">Save menu selection</Button>
    </aside>
  </form></TabsContent>
  <TabsContent value="booking"><form onSubmit={createBooking} className="grid gap-3 sm:grid-cols-2"><div className="space-y-1.5"><Label>Event date</Label><DateField name="event_date" required defaultValue={booking?.event_date||lead.preferred_dates?.[0]||""}/></div><div className="space-y-1.5"><Label>Start time</Label><input type="hidden" name="start_time" value={bookStart}/><TimeDropdownPicker value={bookStart} onChange={setBookStart}/></div><div className="space-y-1.5"><Label>End time</Label><TimeDropdownPicker value={bookEnd} onChange={(v)=>{setBookEnd(v);const d=(timeToMin(v)-timeToMin(bookStart))/60;if(d>0)setDurationHours(d);}}/></div><div><Label>Duration (hours)</Label><Input name="duration" type="number" min="0.5" step="0.5" required value={durationHours} onChange={(e)=>setDurationHours(e.target.value)}/></div><div><Label>Confirmed guests</Label><Input name="guests" type="number" min="1" required defaultValue={booking?.guest_count||lead.estimated_guest_count||1}/></div><div><Label>Venue</Label><OptionSelect name="venue" options={venueOptions} defaultValue={booking?.venue_space||lead.venue_space||""}/></div><div><Label>Total</Label><Input name="total" type="number" min="0" defaultValue={booking?.total_amount||total}/></div><div><Label>Deposit</Label><Input name="deposit" type="number" min="0" defaultValue={booking?.deposit_amount||0}/></div><div className="space-y-1.5"><Label>Deposit due</Label><DateField name="deposit_due" defaultValue={booking?.deposit_due_date||""} placeholder="Not set"/></div><div className="space-y-1.5"><Label>Balance due</Label><DateField name="balance_due" defaultValue={booking?.balance_due_date||""} placeholder="Not set"/></div><div className="flex flex-wrap gap-2 self-end"><Button>{booking?"Update booking":"Prepare booking"}</Button>{booking&&<Button type="button" variant="outline" onClick={downloadPdf}><Download className="mr-2 h-4 w-4"/>PDF</Button>}</div></form><div className="mt-5 space-y-2">{leadTasks.map(t=><p key={t.id} className="text-sm text-muted-foreground">• {t.title}</p>)}</div></TabsContent>
  <TabsContent value="runsheet"><RunsheetTab lead={lead} booking={booking} options={options} onSaved={onSaved}/></TabsContent>
  </Tabs></div></DialogContent></Dialog>;
}